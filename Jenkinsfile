pipeline {
    agent any
    stages {
        stage("build") {
            steps {
                script{
                    // Build steps go here
                    echo "Testing the application..."
                    echo "Executing pipeline for branch $BRANCH_NAME"
                } 
            }
        }
